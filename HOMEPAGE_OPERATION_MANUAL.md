# 마이크온 스피치 홈페이지 운영 사용설명서

이 문서는 홈페이지 운영자가 일상적으로 사용하는 기능을 기준으로 작성한 운영 매뉴얼입니다. 코드 수정이나 개발 작업이 아니라, 팝업 관리, 상담신청 확인, 알림 설정, 기본 점검 방법을 중심으로 설명합니다.

## 1. 접속 주소

- 홈페이지: `https://mikeonspeech.netlify.app`
- 관리자 화면: `https://mikeonspeech.netlify.app/poppop.html`
- 개인정보처리방침: `https://mikeonspeech.netlify.app/privacy.html`
- 이용약관: `https://mikeonspeech.netlify.app/terms.html`

관리자 화면은 비밀번호가 있어야 접속할 수 있습니다. 비밀번호는 문서나 메신저에 공개적으로 남기지 말고, 운영 책임자만 관리하는 것을 권장합니다.

## 2. 관리자 화면 기본 사용법

1. 관리자 화면에 접속합니다.
2. 관리자 비밀번호를 입력합니다.
3. 상단 메뉴에서 필요한 기능을 선택합니다.
   - `팝업 관리`: 홈페이지에 표시되는 공지 팝업을 등록, 수정, 삭제합니다.
   - `상담신청 내역`: 홈페이지 상담신청 폼으로 접수된 내역을 확인합니다.
4. 작업이 끝나면 `로그아웃`을 누릅니다.

관리자 화면에서 저장한 팝업 내용은 별도 배포 없이 홈페이지에 반영됩니다. 단, 홈페이지 디자인, 문구, 섹션 구성 자체를 바꾸는 작업은 별도 수정 및 배포가 필요합니다.

## 3. 팝업 관리

### 3.1 새 팝업 등록

관리자 화면의 `팝업 관리` 탭에서 오른쪽 입력 영역을 작성한 뒤 `저장`을 누릅니다.

입력 항목은 다음과 같습니다.

- `ID`: 팝업을 구분하는 고유값입니다. 예: `new-course-2026-06`
- `제목`: 방문자에게 보이는 팝업 제목입니다.
- `표시 방식`: 팝업 노출 형태입니다.
- `이미지 URL`: 팝업에 사용할 이미지 주소입니다.
- `본문`: 팝업 안내 문구입니다.
- `버튼 문구`: 버튼에 표시할 문구입니다. 예: `상담 예약하기`
- `버튼 링크`: 버튼 클릭 시 이동할 위치입니다.
- `시작일`: 팝업 노출을 시작할 날짜입니다.
- `종료일`: 팝업 노출을 종료할 날짜입니다.
- `우선순위`: 숫자가 클수록 먼저 노출됩니다.
- `활성화`: 체크되어 있어야 홈페이지에 표시됩니다.

### 3.2 표시 방식 선택 기준

`기본 모달 - 한 개씩 순차 표시`

- 중요한 공지 1개를 강하게 보여줄 때 적합합니다.
- 팝업이 여러 개 있으면 화살표로 이전/다음 공지를 이동할 수 있습니다.

`팝업존 카드 - 최대 3개 카드 표시`

- 제목, 이미지, 바로가기 링크가 있는 공지를 여러 개 보여줄 때 적합합니다.
- PC에서는 한 화면에 최대 3개 카드가 보입니다.
- 모바일에서는 1개씩 보이며 화살표로 이동합니다.
- 공지가 4개 이상이면 페이지 이동 화살표가 표시됩니다.

운영 기준으로는 단기 이벤트, 신규 강좌, 모집 안내처럼 이미지가 중요한 공지는 `팝업존 카드`를 권장합니다. 긴급 안내나 단일 공지는 `기본 모달`이 적합합니다.

### 3.3 버튼 링크 예시

- 상담신청 영역으로 이동: `#contact`
- 프로그램 영역으로 이동: `#programs`
- 외부 페이지로 이동: `https://...`

외부 페이지로 연결할 때는 주소 전체를 입력합니다. 홈페이지 내부 특정 영역으로 이동할 때는 `#contact`처럼 섹션 ID를 입력합니다.

### 3.4 이미지 사용 기준

이미지 URL에는 다음 형태를 사용할 수 있습니다.

- 홈페이지 내부 이미지: `assets/example.jpg`
- 공개 이미지 주소: `https://...`

권장 사항:

- 이미지가 외부에서 직접 열리는지 먼저 확인합니다.
- 비공개 블로그, 로그인 필요 페이지, 만료되는 이미지 주소는 피합니다.
- 가능하면 홈페이지 프로젝트의 `assets` 폴더에 이미지를 넣고 `assets/...` 경로로 사용하는 방식이 안정적입니다.
- 저작권이 불명확한 이미지는 사용하지 않는 것이 안전합니다.

### 3.5 팝업 수정

1. `팝업 목록`에서 수정할 팝업의 `수정` 버튼을 누릅니다.
2. 오른쪽 입력 영역에 기존 내용이 채워집니다.
3. 필요한 내용을 변경합니다.
4. `저장`을 누릅니다.

### 3.6 팝업 삭제 또는 비활성화

- 일시적으로 숨기려면 `활성화` 체크를 해제하고 저장합니다.
- 더 이상 사용하지 않을 팝업은 `삭제`를 누릅니다.

운영 전에는 테스트 팝업이 활성화되어 있지 않은지 반드시 확인하는 것이 좋습니다.

### 3.7 팝업이 보이지 않을 때 점검

다음 순서로 확인합니다.

1. 관리자 화면에서 해당 팝업이 `활성` 상태인지 확인합니다.
2. `시작일`이 오늘 이후로 되어 있지 않은지 확인합니다.
3. `종료일`이 이미 지난 날짜가 아닌지 확인합니다.
4. 같은 브라우저에서 `오늘 하루 보지 않기`를 눌렀는지 확인합니다.
5. 다른 브라우저나 시크릿 창에서 홈페이지를 다시 열어봅니다.
6. 이미지가 안 보이는 문제라면 이미지 URL을 새 탭에서 직접 열어봅니다.

## 4. 상담신청 내역 확인

홈페이지 하단 `상담신청` 영역에서 방문자가 내용을 제출하면 Netlify Forms에 저장됩니다. 관리자 화면에서도 확인할 수 있습니다.

### 4.1 관리자 화면에서 확인

1. 관리자 화면에 로그인합니다.
2. 상단의 `상담신청 내역` 탭을 누릅니다.
3. 접수된 내역을 확인합니다.
   - 접수일
   - 이름
   - 연락처
   - 관심 프로그램
   - 상담 내용
4. 새 내역이 보이지 않으면 `새로고침`을 누릅니다.

관리자 화면에서 상담신청 내역을 확인할 수 있으므로, 운영자가 반드시 Netlify 계정에 접속해야만 하는 구조는 아닙니다.

### 4.2 Netlify 관리자에서 확인

Netlify 계정으로도 원본 접수 내역을 확인할 수 있습니다.

1. Netlify에 로그인합니다.
2. `mikeonspeech` 프로젝트를 선택합니다.
3. `Forms` 메뉴로 이동합니다.
4. `consultation` 폼의 제출 내역을 확인합니다.

관리자 화면에 문제가 있거나 원본 데이터를 확인해야 할 때 Netlify의 `Forms` 메뉴를 사용합니다.

### 4.3 상담신청 처리 권장 흐름

1. 매일 1회 이상 `상담신청 내역`을 확인합니다.
2. 신규 신청자에게 전화, 문자, 이메일 등 운영 방식에 맞게 연락합니다.
3. 상담 완료 여부는 별도 고객관리표나 내부 문서에 기록합니다.
4. 개인정보가 포함된 내용은 공개 메신저나 공유 문서에 그대로 복사하지 않습니다.

현재 홈페이지 상담신청 폼에는 이름, 연락처, 관심 프로그램, 상담 내용, 개인정보 동의 항목이 있습니다.

## 5. 상담신청 이메일 알림 설정

Netlify에서 상담신청이 들어올 때 이메일 알림을 받을 수 있습니다.

설정 경로:

1. Netlify에 로그인합니다.
2. `mikeonspeech` 프로젝트로 들어갑니다.
3. `Project configuration`으로 이동합니다.
4. `Notifications` 메뉴를 엽니다.
5. `Emails and webhooks` 영역에서 `Form submission notifications`를 찾습니다.
6. `Add notification`을 누릅니다.
7. `Email notification`을 선택합니다.
8. 폼은 `consultation`을 선택합니다.
9. 알림을 받을 이메일 주소를 입력하고 저장합니다.

설정 후 테스트 상담신청을 1건 제출해 이메일이 도착하는지 확인하는 것이 좋습니다.

## 6. 운영 체크리스트

### 매일 확인

- 신규 상담신청 내역 확인
- 상담신청 이메일 알림 수신 여부 확인
- 홈페이지 첫 화면과 팝업이 정상적으로 보이는지 확인

### 공지 등록 전 확인

- 제목이 너무 길지 않은지 확인
- 이미지가 정상적으로 보이는지 확인
- 버튼 링크가 원하는 위치로 이동하는지 확인
- 시작일, 종료일, 활성화 상태 확인
- 모바일에서도 팝업이 불편하지 않은지 확인

### 행사 또는 모집 종료 후 확인

- 종료된 팝업 비활성화 또는 삭제
- 지난 일정 문구가 홈페이지에 남아 있지 않은지 확인
- 상담신청 내역 중 미처리 건 확인

## 7. 자주 발생하는 문제

### 관리자 로그인이 되지 않음

- 비밀번호가 정확한지 확인합니다.
- 여러 번 실패하면 브라우저를 새로고침한 뒤 다시 시도합니다.
- 계속 실패하면 관리자 비밀번호 환경설정이 변경되었는지 확인이 필요합니다.

### 상담신청 내역이 관리자 화면에 보이지 않음

- 먼저 Netlify `Forms` 메뉴에서 접수 내역이 있는지 확인합니다.
- 관리자 화면에서 `새로고침`을 누릅니다.
- 로그인 세션이 만료되었을 수 있으므로 로그아웃 후 다시 로그인합니다.
- Netlify 원본에는 있는데 관리자 화면에만 안 보이면 서버 설정 점검이 필요합니다.

### 팝업 이미지만 보이지 않음

- 이미지 URL을 새 브라우저 탭에서 직접 열어봅니다.
- 로그인해야 보이는 이미지라면 홈페이지 방문자에게도 보이지 않을 수 있습니다.
- 외부 이미지가 불안정하면 `assets` 폴더에 이미지 파일을 넣어 내부 경로를 사용하는 것이 좋습니다.

### 팝업을 닫았는데 다시 보고 싶음

- `오늘 하루 보지 않기`를 누른 경우 같은 브라우저에서는 당일 다시 보이지 않습니다.
- 다른 브라우저, 시크릿 창, 또는 다음 날 다시 확인합니다.

## 8. 보안 및 개인정보 주의사항

- 관리자 비밀번호는 운영 책임자 외에 공유하지 않습니다.
- 상담신청 내역에는 개인정보가 포함되므로 외부 공유를 최소화합니다.
- 상담신청 데이터를 공개 문서, 공개 채팅방, 외부 캡처 이미지에 그대로 노출하지 않습니다.
- Netlify 계정 권한은 필요한 사람에게만 부여합니다.
- 서버 환경변수나 인증 토큰은 홈페이지 문서, 메신저, 이메일에 적지 않습니다.

## 9. 운영자가 알아두면 좋은 기준

- 팝업 내용 변경은 관리자 화면에서 바로 처리할 수 있습니다.
- 상담신청 확인도 관리자 화면에서 처리할 수 있습니다.
- 디자인 수정, 섹션 문구 변경, 로고 이미지 교체, 개인정보처리방침 내용 수정 등은 파일 수정과 배포가 필요합니다.
- 공지 이미지나 로고는 저작권과 사용 허락을 확인한 뒤 사용하는 것이 안전합니다.
- 운영 전 최종 확인은 PC와 모바일 화면을 모두 보는 것이 좋습니다.

